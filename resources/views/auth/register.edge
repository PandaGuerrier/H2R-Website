<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Harcèlement2Rue</title>
  @entryPointStyles('app')
  @entryPointScripts('app')
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
  <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
  <script src="https://www.google.com/recaptcha/api.js" async defer></script>
</head>

<body>
  <main class="h-[100vh]">
    @!navbar()
    <!-- Header -->

    <div class="min-h-full flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8 z-30">
      <div class="max-w-md w-full space-y-8 z-30 bg-white border p-10 rounded-md">
        <div>
          <h2 class="mt-6 text-center text-3xl tracking-tight font-bold text-purple-500">Inscription</h2>
          <p class="mt-2 text-center text-sm text-black">
            Ou
            <a href="/auth/login" class="font-medium text-purple-300 hover:text-purple-400 duration-200"> j'ai un compte </a>
          </p>
        </div>

        <!-- Début du form -->

        <form id="demo-form" class="mt-8 space-y-6" action="{{ route('register') }}" method="POST">
          <div class="rounded-md shadow-sm space-y-5">
            @if(flashMessages.has('notification'))
            @!component('components/actionbar', {
            type: 'error',
            text: flashMessages.get('notification'),
            })
            @endif
            <div>
              <label for="username" class="block text-sm font-medium text-purple-500">Pseudo</label>
              <div class="relative mt-1 rounded-md shadow-sm">
                <input type="text" name="username"
                  class="block w-full rounded-full border-gray-200 pr-12 focus:ring-purple-500 focus:ring-offset-purple-300 sm:text-sm">
              </div>
              @if(flashMessages.has('errors.username'))
              <h1 class="text-red-500 text-sm">{{ flashMessages.get('errors.username') }}</h1>
              @endif
            </div>
            <div>
              <label for="email" class="block text-sm font-medium text-purple-500">Email</label>
              <div class="relative mt-1 rounded-md shadow-sm">
                <input type="email" name="email"
                  class="block w-full rounded-full border-gray-200 pr-12 focus:ring-purple-500 focus:ring-offset-purple-300 sm:text-sm">
              </div>
              @if(flashMessages.has('errors.email'))
              <h1 class="text-red-500 text-sm">{{ flashMessages.get('errors.email') }}</h1>
              @endif
            </div>
            <div>
              <label for="password" class="block text-sm font-medium text-purple-500">Mot de passe</label>
              <div class="relative mt-1 rounded-md shadow-sm">
                <input type="password" name="password"
                  class="block w-full rounded-full border-gray-200 pr-12 focus:ring-purple-500 focus:ring-offset-purple-300 sm:text-sm">
              </div>
              @if(flashMessages.has('errors.password'))
              <h1 class="text-red-500 text-sm">{{ flashMessages.get('errors.password') }}</h1>
              @endif
            </div>
            <div>
              <label for="password_confirmation" class="block text-sm font-medium text-purple-500">Répétez le mot de
                passe</label>
              <div class="relative mt-1 rounded-md shadow-sm">
                <input type="password" name="password_confirmation"
                  class="block w-full rounded-full border-gray-200 pr-12 focus:ring-purple-500 focus:ring-offset-purple-300 sm:text-sm">
              </div>
              @if(flashMessages.has('errors.password_confirmation'))
              <h1 class="text-red-500 text-sm">{{ flashMessages.get('errors.password_confirmation') }}</h1>
              @endif
            </div>
          </div>

          <button id="login-button" data-callback="onSubmit" data-sitekey="6Le_gYQiAAAAAFuNVAqShsTnDGOskGgmqW3QL6kx"
          class="g-recaptcha duration-200 group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-full text-white bg-purple-700 hover:bg-purple-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-700">
            <span class="absolute left-0 inset-y-0 flex items-center pl-3">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none"
                stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                class="feather feather-user-plus">
                <path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                <circle cx="8.5" cy="7" r="4"></circle>
                <line x1="20" y1="8" x2="20" y2="14"></line>
                <line x1="23" y1="11" x2="17" y2="11"></line>
              </svg> </span>
            Inscription
          </button>
        </form>
        <div>
        </div>
  </main>
</body>
<script>
  function onSubmit(token) {
      document.getElementById("demo-form").submit();
    }
</script>

</html>